@*Adminde tanımlanan partial viewsler kullanılmadı bunlar silinebilir*@
<!DOCTYPE html>
<html>

@await Html.PartialAsync("/Views/Dashboard/HeadPartial.cshtml")
<body>
    <!-- Sidenav -->
@await Html.PartialAsync("/Views/Dashboard/SideNavbarPartial.cshtml")

    <div class="main-content" id="panel">
        <!-- Topnav -->
@*         @await Html.PartialAsync("/Views/Admin/TopNavbarPartial.cshtml")
 *@      @await Component.InvokeAsync("DashboardTopNavbar")   <!-- Header -->
@*         @await Html.PartialAsync("/Views/Admin/HeaderPartial.cshtml")
 *@        
        @await Component.InvokeAsync("DashboardHeaderComponent")
     
            <!-- Page content -->
        <div class="container-fluid mt-6">
            <div class="row">
                <div class="col">
                    <div class="card border-0">
@*                         <div id="map-default" class="map-canvas" data-lat="40.748817" data-lng="-73.985428" style="height: 600px;">Page Content</div>
 *@     
                        @RenderBody()

                    </div>
                    @await RenderSectionAsync("scripts", required: false)
                </div>
            </div>
            <!-- Footer -->
@*             @await Html.PartialAsync("/Views/Admin/FooterPartial.cshtml")
 *@            @await Html.PartialAsync("/Views/Dashboard/FooterPartial.cshtml")
        </div>
    </div>    <!-- Argon Scripts -->
    <!-- Core -->
@*     @await Html.PartialAsync("/Views/Admin/ScriptPartial.cshtml");
 *@    @await Html.PartialAsync("/Views/Dashboard/ScriptPartial.cshtml");
   


</body>

</html>
